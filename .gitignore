.DS_Store
.env
*.env
*.pem
*.key
*.log
__pycache__/
node_modules/
.venv/
dist/
build/
.project-setup/

# Rust (Task T01)
/target/
**/target/

# Internal Hekton factory state, untracked from this PUBLIC repo (2026-07-19 doc-safety
# audit). This handles ONLY these two dirs; the broader tree sanitisation (machine-path
# relativisation, coderturtle<->dermdunc prose de-linkage) is a separate ongoing pass, and
# NONE of this purges git HISTORY — the content remains in prior commits until a deliberate
# history remediation (see the Repo Sanitisation workflow). Not "closed".
# - .control-tower/: orchestrator run transcripts/ledgers
# - mind-palace/: repo-local Obsidian vault mirror (belongs to the vault-sync flow, not the
#   public product repo). Both kept locally, synced via scripts.
.control-tower/
mind-palace/
