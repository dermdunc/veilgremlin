---
task_id: T05
task_type: code-implement
privacy: vendor-allowed
engine: claude-cli
repo: /Users/hekton/Development/hekton/factory-output/veilgremlin
context:
  - docs/architecture/interface-contracts.md
---

## Task Description

SQLCipher vault: schema (mapping, demask_event), OS-keychain key wrap (macOS first), zeroize, TTL/purge, prepared-statement reuse.

## Acceptance

encrypted at rest; key never plaintext; lookup bench within budget; TTL purge works. ADDED 2026-07-17: VaultStore::intern must call into crates/vg-core/src/keying.rs's Keyer/placeholder_key (Task T04) rather than reimplementing keying, AND must reseed each namespace's ordinal counters from the vault's own persisted records at construction time -- a fresh in-memory Keyer after a process restart must not hand out a display ordinal (e.g. EMAIL_001) that collides with or diverges from what the persisted vault already recorded for a different value. This was found as a real gap during T04's own review, not designed for at the time -- do not skip it.

## Definition of Done

- [ ] `cargo build --locked` clean; `cargo clippy -- -D warnings` clean; `cargo fmt --check` clean.
- [ ] Unit tests cover the happy path + the failure modes named in the spec.
- [ ] Hot-path changes: a criterion bench exists and p95 is within budget; no network/ML added.
- [ ] Privacy-behaviour changes: a test asserts the relevant Go/No-Go invariant.
- [ ] Public interface matches `interface-contracts.md`, or went through the contract-change protocol.
- [ ] Crate-level docs / rustdoc updated.
- [ ] `docs/decisions.md`, `docs/session-log.md`, `docs/next-actions.md` updated.
- [ ] Session-scoped commit (explicit paths only); PR opened.
