# VeilGremlin build DAG — the machine source of truth for the task-DAG
# orchestrator (agentic-control-tower's `dag` commands). Transcribed once from
# docs/architecture/work-breakdown.md's table + compact DAG diagram; edit
# THIS file if the plan changes, not the markdown (which stays the
# human-readable narrative — re-run `dag gen-specs` after any edit here to
# regenerate the corresponding engine-gateway-lab task specs).
#
# Wave-B tasks depend on BOTH T01 and T02, not just T02 (the work-breakdown
# table's "Blocks on" column lists only the contract dependency, T02) --
# agent-factory-plan.md's own rule is a WAVE gate ("dispatch all of Wave B in
# one batch once T01+T02 merge"), so both are encoded here as depends_on.
#
# Engine choice for V1 (see engine-gateway-lab's Phase 15 decision): cloud-only.
# claude-cli for build tasks; codex-cli for T11 (an independent reviewer, not
# the same engine that wrote the code) -- matches routing-policy.yaml's own
# code-review -> codex-cli default. privacy = vendor-allowed throughout:
# VeilGremlin's own source isn't privacy-sensitive (see .hekton/project.yaml,
# privacy_boundary: internal); vendor-allowed lets gateway-run.sh dispatch
# unattended (no per-run confirmation prompt) -- human review happens at the
# control-tower session review/close step instead.

[orchestrator]
gateway_run = "/Users/hekton/Development/hekton/labs/engine-gateway-lab/scripts/gateway-run.sh"
repo = "/Users/hekton/Development/hekton/factory-output/veilgremlin"
default_verify = "cargo build --locked && cargo clippy --all-targets -- -D warnings && cargo fmt --check && cargo test"
default_privacy = "vendor-allowed"
spec_dir = ".hekton/build-tasks"

# --- Epic E1 — Foundation & supply chain (Wave A) ---------------------------

[[task]]
id = "T01"
wave = "A"
depends_on = []
task_type = "code-implement"
engine = "claude-cli"
description = "Cargo workspace + crate skeletons; CI (fmt, clippy -D warnings, cargo-deny, cargo-audit, --locked); bench CI job; release skeleton (SBOM stub, signing stub)."
acceptance = "cargo build green on empty crates; CI runs on PR; cargo-deny config present."
context = ["docs/architecture/agent-factory-plan.md"]
verify_command = "cargo build --locked && cargo fmt --check"

[[task]]
id = "T02"
wave = "A"
depends_on = []
task_type = "code-implement"
engine = "claude-cli"
description = "Freeze shared types + library API in vg-core; define trait seams (Detector, Parser, VaultStore, PolicyEngine, AuditSink); write contract-conformance test stubs."
acceptance = "interface-contracts.md v1 frozen; types compile; conformance test scaffold exists."
context = ["docs/architecture/interface-contracts.md", "docs/architecture/agent-factory-plan.md"]

# --- Epic E2 — Detection & masking primitives (Wave B, parallel) -----------

[[task]]
id = "T03"
wave = "B"
depends_on = ["T01", "T02"]
task_type = "code-implement"
engine = "claude-cli"
# 2026-07-15: expanded after the real T03 pilot dispatch returned a clarifying
# question instead of doing the work -- headless -p mode has no follow-up
# channel, so a terse one-liner was ambiguous enough that claude asked rather
# than acted. Concrete file/module/trait guidance below, plus an explicit
# "use your best judgment, don't ask" instruction, replaces the original
# one-line description (kept in git history: see this file's blame).
description = """
Implement deterministic detectors in `crates/vg-detectors/src/` (currently an empty stub crate
-- see its `lib.rs` docstring: "detector implementations land in Tasks T03/T04"; this task is T03).
Each detector implements `vg_core::traits::Detector` (`fn id(&self) -> DetectorId`, `fn detect(&self,
buf: &[u8], spans: &[Span]) -> Vec<Finding>`, `fn entity_types(&self) -> &[EntityType]` -- see
`crates/vg-core/src/traits.rs`).

Build these five detectors, one module per detector under `crates/vg-detectors/src/`:

1. `email.rs` -- RFC-5322-ish email address regex.
2. `phone.rs` -- E.164-ish phone number regex (accept common separators).
3. `ip.rs` -- IPv4 and IPv6 address regex.
4. `iban_sortcode.rs` -- IBAN (ISO 13616) and UK sort-code patterns.
5. `entropy.rs` -- Shannon-entropy-based detector for high-entropy strings (e.g. API keys/secrets)
   above a tunable threshold, on whitespace/punctuation-delimited tokens.

Use `regex::Regex` (already a workspace dependency) for 1-4, compiled once (`OnceLock` or
equivalent, not per-call). If a dictionary-matching detector is warranted per the interface
contract's Aho-Corasick note, use the `aho-corasick` crate (add as a `vg-detectors` dependency if
not already present) for known-term lists; if no such list is needed for these five, that's fine --
do not add a dictionary detector that isn't specified above.

Wire all five into a `pub fn all_detectors() -> Vec<Box<dyn Detector>>` (or the equivalent
composition point the interface contract specifies) so downstream code can enumerate them.

This is a one-shot headless dispatch: there is no follow-up channel. If any exact naming/shape
choice above is ambiguous, use your best judgment, follow the existing conventions in
`crates/vg-core` and the frozen `interface-contracts.md`, and record the assumption in the PR
description and `docs/decisions.md` rather than asking a clarifying question -- an unasked-but-
documented judgment call is recoverable in review; a session that ends by asking a question is not.
"""
acceptance = "unit tests per detector; bench p95 < 25 ms on reference input."
context = ["docs/architecture/interface-contracts.md"]

[[task]]
id = "T04"
wave = "B"
depends_on = ["T01", "T02"]
task_type = "code-implement"
engine = "claude-cli"
# 2026-07-16: added a real cross-crate integration requirement after the T03 pilot
# (dogfooding/latency review). T04's whole job is "take a Finding, produce a
# placeholder" -- T03 (already closed) is the only real source of Findings that
# exists. Testing T04 only against hand-built mock Findings would mean the FIRST
# time it ever sees a real one is T07's full pipeline wiring, several tasks later.
# 2026-07-17: expanded description with concrete file/module guidance plus an
# explicit "use your best judgment, don't ask" instruction, same fix that
# unblocked T03's first dispatch (a terse one-liner got a clarifying question
# back from headless -p mode instead of code). Kept the original one-liner in
# git history: see this file's blame.
description = """
Implement typed-placeholder + HMAC keying logic, most likely as a new module
`crates/vg-core/src/keying.rs` (vg-core is where Task T02 put all other shared/canonical
logic -- see `crates/vg-core/src/{types,traits,api}.rs` -- and Task T05's not-yet-built
`vg-vault` crate will later call into this from its `VaultStore::intern` implementation;
this task does not need `vg-vault` to exist to be buildable or testable standalone).
`interface-contracts.md`'s `VaultStore` doc comment is the authority on the formula: "stable
placeholder via salted HMAC over `(canonical(value), ty, ns)`" -- see
`crates/vg-core/src/traits.rs` around the `VaultStore` trait and `crates/vg-core/src/types.rs`
for `Placeholder { display: String, mapping_ref: MappingRef }`, `EntityType`, and `Namespace`.

Concretely, this task needs:

1. A `canonicalize(value: &str) -> String` (or similar) normalisation step before hashing --
   e.g. trimming and case-folding where the entity type calls for it -- so trivial
   formatting differences (whitespace, case) don't produce different placeholders for what
   is semantically the same value.
2. A salted HMAC keying function over `(canonical(value), EntityType, Namespace)` (the
   `hmac` + `sha2` crates are reasonable choices; add as `vg-core` dependencies if not
   already present) producing a stable, deterministic key: the same input always yields the
   same key, and a value interned under one `Namespace` must key differently than the same
   value under a different one (this task does not implement namespace *isolation on lookup*
   -- that's `VaultStore::resolve`'s job, in T05 -- but the keying function itself must be
   namespace-sensitive as an input, or T05 has nothing to isolate).
3. Per-type ordinal assignment so a `Placeholder`'s `display` string looks like `EMAIL_001`,
   `ACCOUNT_ID_014` (see `README.md`'s own example) -- the first time a distinct key is seen
   for a given `EntityType` within a `Namespace`, it gets the next sequential number for that
   type; the same key seen again returns the same ordinal.
4. Luhn checksum (for card-number-shaped values) and mod-97 checksum (for IBAN-shaped values)
   validators, used so a placeholder's own display value can be checked (or constructed) to
   remain checksum-valid -- a masked pack that replaces a card number with something that
   fails Luhn on its face is a worse debugging experience than one that doesn't.
5. A session-scoped in-memory cache (a `HashMap` keyed on the same inputs as step 2 is
   reasonable) so repeated keying of the same value within one run doesn't recompute the HMAC
   each time. This is a fast front-cache, not a replacement for T05's persistent vault.

This is a one-shot headless dispatch: there is no follow-up channel. If any exact naming,
module boundary, or type-signature choice above is ambiguous, use your best judgment, follow
the existing conventions already frozen in `crates/vg-core` and `interface-contracts.md`, and
record the assumption in the PR description and `docs/decisions.md` rather than asking a
clarifying question -- an unasked-but-documented judgment call is recoverable in review; a
session that ends by asking a question is not.
"""
acceptance = "same value -> same placeholder within namespace; checksum tests pass. ADDED 2026-07-16: at least one integration test must feed real Findings from vg-detectors::all_detectors() (already built, Task T03) through this task's keying logic end to end -- not only hand-built mock Finding values -- to catch a real interface/shape mismatch (e.g. field content, confidence ranges, entity-type coverage) before Task T07's pipeline wiring, not only at it."
context = ["docs/architecture/interface-contracts.md"]

[[task]]
id = "T08"
wave = "B"
depends_on = ["T01", "T02"]
task_type = "code-implement"
engine = "claude-cli"
# 2026-07-16: added a real cross-crate integration requirement (same rationale as
# T04's, see its comment above). This one is the higher-risk pairing: every
# detector in vg-detectors (Task T03, already closed) already implements
# `fn detect(&self, buf: &[u8], spans: &[Span]) -> Vec<Finding>` -- but all five
# CURRENTLY IGNORE the spans parameter entirely (`_spans` in every signature,
# confirmed 2026-07-16). Nobody has yet exercised a real Span this task produces
# against a real detector's actual (currently no-op) handling of it. This is
# exactly the kind of interface-shape-matches-but-behavior-doesn't-connect risk
# that only surfaces when two independently-built crates are wired together for
# real, and T07 (pipeline wiring) is the earliest point that would otherwise happen.
# 2026-07-17: expanded with concrete guidance before the Wave B concurrent dispatch,
# same fix that unblocked T03/T04 (a terse one-liner risks a clarifying-question
# dispatch failure in headless mode, which has no follow-up channel).
description = """
Implement `vg_core::traits::Parser` in `crates/vg-parsers/src/` (currently an empty stub
crate -- see its `lib.rs` docstring). The trait: `fn can_parse(&self, artefact: &ArtefactHint)
-> bool` and `fn parse(&self, buf: &[u8]) -> ParseResult` where `ParseResult { spans:
Vec<Span>, artefact_kind: ArtefactKind }` (see `crates/vg-core/src/traits.rs` and
`types.rs` for `Span`, `ArtefactHint`, `ArtefactKind`).

Build one module per format under `crates/vg-parsers/src/`: `json.rs`, `yaml.rs` (add
`serde_yaml` or similar), `toml.rs`, `csv.rs`, `log.rs` (generic log-line parsing), `diff.rs`
(git unified diff), `env.rs` (`.env` files), plus tree-sitter support for one source
language of your choice (add the relevant `tree-sitter`/`tree-sitter-<lang>` crates) --
if the choice is ambiguous, pick something simple and common (e.g. Rust) rather than
asking.

The hard contract, not optional: **never panic on malformed input** -- return best-effort
spans instead. Test this with genuinely adversarial buffers (empty, truncated UTF-8,
unbalanced delimiters/brackets, a JSON file that's actually binary), not just one
happy-path fixture per format. Use `vg_core::conformance::assert_parser_never_panics`
for this.

Cross-crate integration requirement (interface-contracts.md, added 2026-07-16): at least
one integration test must feed this task's real `Span` output into
`vg_detectors::all_detectors()` (Task T03, already merged) on a realistic fixture, and
explicitly assert/record in `docs/decisions.md` whether the detectors' `spans` parameter
currently being a no-op (`_spans` in every detector's signature, confirmed 2026-07-16) is
an expected stage-appropriate gap or a real one -- don't let this go unnoticed until T07.

This is a one-shot headless dispatch: there is no follow-up channel. If any exact
naming/shape/language choice above is ambiguous, use your best judgment, follow existing
`vg-core`/`interface-contracts.md` conventions, and record the assumption in
`docs/decisions.md` rather than asking a clarifying question.
"""
acceptance = "parser tests on fixtures; spans typed; error-tolerant on malformed input. ADDED 2026-07-16: at least one integration test must feed this task's real Span output into vg-detectors::all_detectors() (Task T03) on a realistic fixture, and explicitly assert/record whether the detectors' current spans parameter being a no-op is expected at this stage or a real gap worth flagging in docs/decisions.md -- don't let this go unnoticed until Task T07."
context = ["docs/architecture/interface-contracts.md"]

# --- Epic E3 — State, policy, evidence (Wave B, parallel) ------------------

[[task]]
id = "T05"
wave = "B"
depends_on = ["T01", "T02", "T04"]
task_type = "code-implement"
engine = "claude-cli"
# 2026-07-17: added T04 as a hard dependency (was T01/T02 only) after a Codex
# cross-model doubt-pass on T04 found a real cross-task interface gap this task
# must satisfy -- see the acceptance addendum below and docs/decisions.md's T04
# entry for the full finding.
# 2026-07-17: expanded with concrete guidance before the Wave B concurrent dispatch,
# same fix that unblocked T03/T04.
description = """
Implement `vg_core::traits::VaultStore` in `crates/vg-vault/src/` (currently an empty stub
crate). The trait: `fn intern(&self, value: &Secret, ty: EntityType, ns: &Namespace) ->
Result<Placeholder, VaultError>`, `fn resolve(&self, p: &Placeholder, ns: &Namespace) ->
Result<Secret, VaultError>`, `fn purge_expired(&self) -> Result<usize, VaultError>` (see
`crates/vg-core/src/traits.rs`).

Concretely:
1. SQLCipher-backed storage -- `rusqlite` with a bundled-sqlcipher feature is a reasonable
   choice (add as a dependency); schema needs at least a `mapping` table (encrypted
   value <-> placeholder, namespace, entity type, created/expiry timestamps) and a
   `demask_event` table.
2. OS-keychain key wrap, macOS first -- the `keyring` crate (or `security-framework`
   directly) is a reasonable choice for wrapping the DB encryption key; never persist it
   plaintext.
3. `Secret` (from `vg-core`) already zeroizes on drop -- use it for anything holding a raw
   value; don't reintroduce a separate non-zeroizing string path.
4. `intern` **must call into `crates/vg-core/src/keying.rs`'s `Keyer`/`placeholder_key`**
   (Task T04, already merged) for the actual keying formula, rather than reimplementing
   HMAC/canonicalisation logic here. **Hard requirement found during T04's own review:**
   whatever `Keyer` instance backs this vault must have its per-`(Namespace, EntityType)`
   ordinal counters reseeded from this vault's own persisted `mapping` rows at
   construction time -- a fresh in-memory `Keyer` after a process restart must not hand
   out a display ordinal (e.g. `EMAIL_001`) that collides with or diverges from what the
   vault already recorded for a different value. Do not skip this.
5. Namespace isolation is part of the contract, not a convention: `resolve` must return
   `VaultError::NotFound` (not distinguish "wrong namespace" from "doesn't exist") when
   `ns` doesn't match the namespace a placeholder was interned under. Use
   `vg_core::conformance::assert_vault_roundtrip` -- it checks this explicitly.
6. TTL/purge: `purge_expired` removes rows past their TTL. Prepared-statement reuse: cache
   prepared statements on the struct rather than re-preparing per call (this is on the
   hot lookup path per the interface contract).

This is a one-shot headless dispatch: there is no follow-up channel. If any exact
naming/schema/crate choice above is ambiguous, use your best judgment, follow existing
`vg-core`/`interface-contracts.md` conventions, and record the assumption in
`docs/decisions.md` rather than asking a clarifying question.
"""
acceptance = "encrypted at rest; key never plaintext; lookup bench within budget; TTL purge works. ADDED 2026-07-17: VaultStore::intern must call into crates/vg-core/src/keying.rs's Keyer/placeholder_key (Task T04) rather than reimplementing keying, AND must reseed each namespace's ordinal counters from the vault's own persisted records at construction time -- a fresh in-memory Keyer after a process restart must not hand out a display ordinal (e.g. EMAIL_001) that collides with or diverges from what the persisted vault already recorded for a different value. This was found as a real gap during T04's own review, not designed for at the time -- do not skip it."
context = ["docs/architecture/interface-contracts.md"]

[[task]]
id = "T05b"
wave = "B"
depends_on = ["T01", "T02"]
task_type = "code-implement"
engine = "claude-cli"
# 2026-07-17: expanded with concrete guidance before the Wave B concurrent dispatch,
# same fix that unblocked T03/T04.
description = """
Implement `vg_core::traits::AuditSink` in `crates/vg-audit/src/` (currently an empty stub
crate). The trait: `fn write(&self, event: AuditEvent) -> Result<AuditId, AuditError>`,
`fn get(&self, id: AuditId) -> Option<AuditEvent>` (see `crates/vg-core/src/traits.rs`
and `audit.rs` for the `AuditEvent` enum: `Scan`, `PolicyDecision`, `MappingCreated`,
`Block`, `DemaskRequest`, `DemaskDecision`, plus others -- check the actual enum
definition, the interface-contracts.md sketch is illustrative not exhaustive).

Append-only storage: a simple append-only file (JSON Lines, one event per line, fsynced)
is a reasonable, dependency-light choice; SQLite (matching T05's vault) is also
reasonable if you want one storage technology across both. Either is fine -- pick one and
record the choice, don't ask.

**The hard, security-relevant contract:** no raw value in any `AuditEvent` variant, ever
-- only refs/counts/versions. Test this with
`vg_core::conformance::assert_audit_event_excludes_raw_values`, which checks both the
literal raw value AND its `Debug`-escaped form (a raw value containing control
characters renders escaped in `{event:?}`, so checking only the unescaped literal would
miss exactly that class of leak -- read the helper's own doc comment before assuming a
simpler check suffices). Version every record type so the schema can evolve later without
breaking old records.

This is a one-shot headless dispatch: there is no follow-up channel. If any exact
naming/storage/schema choice above is ambiguous, use your best judgment, follow existing
`vg-core`/`interface-contracts.md` conventions, and record the assumption in
`docs/decisions.md` rather than asking a clarifying question.
"""
acceptance = "events written; property test: no raw value ever serialised."
context = ["docs/architecture/interface-contracts.md"]

[[task]]
id = "T06"
wave = "B"
depends_on = ["T01", "T02"]
task_type = "code-implement"
engine = "claude-cli"
# 2026-07-17: expanded with concrete guidance before the Wave B concurrent dispatch,
# same fix that unblocked T03/T04.
description = """
Implement `vg_core::traits::PolicyEngine` in `crates/vg-policy/src/` (currently an empty
stub crate). The trait: `fn load(layers: PolicyLayers) -> Result<Self, PolicyError> where
Self: Sized`, `fn classify_artefact(&self, hint: &ArtefactHint) -> HandlingClass`,
`fn classify_entity(&self, ty: EntityType) -> HandlingClass`,
`fn destination_allows_masked_only(&self, dest: &DestinationId) -> bool`,
`fn demask_allowed(&self, dest: Destination, actor: &Actor) -> bool`,
`fn version(&self) -> &str` (see `crates/vg-core/src/traits.rs` for the exact trait and
`api.rs`/`types.rs` for `PolicyLayers { global: PathBuf, repo: Option<PathBuf>, session:
Option<PathBuf> }`, `HandlingClass`, `Destination`, `DestinationId`, `Actor`).

Concretely:
1. Pick a config format for policy packs -- YAML (`serde_yaml`) or TOML (`toml`, already
   used elsewhere in this workspace) are both reasonable; add the dependency and write at
   least one example policy file (fixture) that `load` can actually parse, per the
   acceptance criterion.
2. 3-layer resolution: session overrides repo overrides global. Write a real test with
   all three layers present and overlapping keys, not just one layer.
3. Signed-pack verification is a deliberate **stub** in Phase 1 -- don't build real
   signature verification, just a clearly-marked placeholder that always accepts (comment
   why, and that Phase 2 replaces it).
4. **The one hard-deny rule that must be real, not stubbed:** `demask_allowed` must return
   `false` for `Destination::RemoteModelPrompt` and `Destination::ObservabilitySink`
   regardless of actor, in the default policy. Use
   `vg_core::conformance::assert_policy_engine_denies_hard_deny_destinations` -- this is
   the one piece of this task that's actually security-load-bearing, the rest is
   configuration plumbing.

This is a one-shot headless dispatch: there is no follow-up channel. If any exact
naming/format/schema choice above is ambiguous, use your best judgment, follow existing
`vg-core`/`interface-contracts.md` conventions, and record the assumption in
`docs/decisions.md` rather than asking a clarifying question.
"""
acceptance = "example policy parses; layering resolves; deny rules enforced in tests."
context = ["docs/architecture/interface-contracts.md"]

# --- Epic E4 — Pipeline & integration (Wave C) ------------------------------

[[task]]
id = "T07"
wave = "C"
depends_on = ["T03", "T04", "T05", "T05b", "T06", "T08"]
task_type = "code-implement"
engine = "claude-cli"
# 2026-07-18: expanded with concrete guidance before dispatch (same fix as
# T03-T08: a terse one-liner risks a clarifying-question failure in headless
# mode). This spec also banks three hard requirements found during the Wave B
# reviews (see docs/decisions.md 2026-07-17 T05/T06/T08 entries) so the
# integration task cannot silently miss them.
description = """
Wire the masking pipeline in `crates/vg-core/src/api.rs`: replace the `todo!()` bodies of
`scan()` and `mask()` with real implementations. Everything they compose already exists and is
merged: detectors (T03, `vg_detectors::all_detectors()`), keying (T04, `vg_core::keying`),
vault (T05, `vg_vault::Vault`), audit sink (T05b, `vg_audit::JsonlAuditSink`), policy engine
(T06, `vg_policy::LayeredPolicyEngine`), parsers (T08, `vg_parsers`). `vg-core` reaches them
only through its own trait objects (`Context { parsers, detectors }`, `Policy { engine, vault,
audit }`) -- do NOT add a vg-core dependency on any implementing crate except as
dev-dependencies for integration tests (the T04 precedent).

**A known contract-shape gap you will hit immediately, resolve it this way:** the frozen
`mask(input, policy, ns)` signature has no `Context` parameter, so `mask` cannot reach the
detectors/parsers that `scan` gets via `ctx`. Resolve via the contract-change protocol
(`docs/architecture/agent-factory-plan.md` section 6): add `ctx: &Context` to `mask` (making it
`mask(input, ctx, policy, ns)`), update `docs/architecture/interface-contracts.md` section 2
with a versioned note recording the change and why, and update the conformance stubs if they
reference the old shape. Do not smuggle detectors into `Policy` and do not have callers
pre-compute findings -- the explicit ctx parameter is the sanctioned fix.

Pipeline order inside `mask`:
1. Classify the artefact first: `policy.engine.classify_artefact(&input.hint)`. If `Block`
   (e.g. a `.env` file per the default policy fixture), write an `AuditEvent::Block` via
   `policy.audit`, and return a `MaskedPack` whose `text` is empty/refused with
   `stats.blocked_artefacts = 1` -- the artefact's content must never appear in the pack.
2. **Hard requirement (T06 review): artefact `Pass` means "send after entity masking", NEVER
   "skip detection".** Entity scanning below runs for every non-Block artefact regardless of
   its artefact class, or the `Pass` default becomes a fail-open leak.
3. Parse: pick the first parser whose `can_parse(&input.hint)` is true; run it for spans.
   **Hard requirement (T08 review): spans are enrichment only -- detectors scan the FULL raw
   buffer, not span-gated regions.** Two known under-spanning reproducers are documented in
   docs/decisions.md (a `#` inside a quoted YAML value; single-quoted YAML flow scalars); if
   you gate detection on spans those become missed-secret bugs. Pass spans through to
   `Detector::detect(buf, spans)` as the trait allows, but never restrict what gets scanned.
4. Detect: run every detector in `ctx.detectors` over the buffer; collect Findings.
5. Resolve overlapping findings before masking: when two findings' spans overlap, keep the
   more specific entity type over the generic one (e.g. an `Email` finding wins over an
   `entropy` `Secret` covering the same bytes; T03's docs anticipate exactly this), otherwise
   keep the longer span. Never double-mask the same bytes.
6. Per finding, classify: `policy.engine.classify_entity(ty)` ->
   - `Mask`: `policy.vault.intern(&Secret::new(value), ty, ns)` -> replace the span with the
     returned placeholder display; collect the `MappingRef`.
   - `IrreversibleRedact`: replace with a fixed typed marker (e.g. `[REDACTED:PASSWORD]`);
     **never call intern** -- the acceptance criterion "irreversible class never vault-stored"
     is tested, not assumed.
   - `Block` (entity-level): treat as IrreversibleRedact for the span (do not fail the whole
     artefact for one entity) and record it in stats.
   - `Pass`: leave the bytes untouched.
   Apply replacements back-to-front (descending span order) so earlier offsets stay valid.
7. Build the `MaskedPack { text, mapping_refs, stats, policy_version }` and ONE
   `AuditEvent::Scan` (counts by entity type, detector version, latency_us measured around the
   detect step); write it via `policy.audit` and also return it per the signature.
8. **Hard requirement (T05 review): demask logging has one owner.** The vault's own
   `demask_event` table already records every `resolve` attempt. When T07 wires the allowed
   path of `rehydrate` (`policy.vault.resolve` for non-hard-deny destinations after
   `policy.engine.demask_allowed` passes), do NOT also emit `AuditEvent::DemaskDecision` from
   the pipeline -- record in docs/decisions.md that the vault owns demask attribution in
   Phase 1, and leave a comment at the call site saying so.

Tests (integration, in `crates/vg-core/tests/`, with dev-dependencies on the real crates):
- End-to-end mask on a realistic mixed fixture (emails + IBAN + a secret + a date + paths)
  through real detectors/parsers/policy fixtures/a temp-keyed real vault/a temp audit sink:
  same value -> same placeholder; `MaskedPack.text` contains placeholders, not values.
- `vg_core::conformance::assert_masked_pack_excludes_raw_values` as a property test over the
  fixture's raw values (the invariant is tested, not type-enforced -- every MaskedPack field
  is pub).
- An `.env`-hinted input is Blocked: audit event written, no content in the pack, nothing
  interned.
- An IrreversibleRedact-classed value (e.g. a password) is redacted and provably absent from
  the vault (mapping_count stays 0 for it).
- An e2e criterion bench (`benches/`) for `mask()` on the reference buffer against the 25ms
  p95 budget, plus extending the existing plain-#[test] latency gate pattern
  (crates/vg-detectors/tests/latency_gate.rs is the precedent) to cover the full pipeline.

This is a one-shot headless dispatch: there is no follow-up channel. If any exact
naming/shape choice is ambiguous, use your best judgment, follow the frozen contracts and the
existing conventions in vg-core, and record the assumption in docs/decisions.md rather than
asking a clarifying question.
"""
acceptance = "end-to-end mask on fixture; irreversible class never vault-stored; e2e bench. ADDED 2026-07-18 from the Wave B reviews: (1) artefact Pass must not skip entity detection (T06); (2) detectors scan the full raw buffer, spans are enrichment only (T08); (3) the vault owns demask attribution -- no DemaskDecision double-logging from the pipeline (T05); (4) the mask signature change (adding ctx) goes through the contract-change protocol with interface-contracts.md updated, not made silently."
context = ["docs/architecture/interface-contracts.md"]

[[task]]
id = "T09"
wave = "C"
depends_on = ["T07"]
task_type = "code-implement"
engine = "claude-cli"
# 2026-07-16: added an explicit UX-latency criterion after the T03 pilot's
# dogfooding/latency review. Every acceptance criterion up to this point is
# functional (round trip works, gate denies correctly, help text exists) -- none of
# them test the actual product goal that this is meant to be an INVISIBLE control.
# T09 is the earliest point that goal is even testable for real (hooks wired into a
# real Claude Code session), so it shouldn't ship without someone actually noticing
# whether it's noticeable.
# 2026-07-18: expanded with concrete guidance before dispatch (the T03-T07 fix for
# terse one-liners), banking the T07-review hard requirement (demask via
# MappingRefs, never placeholder-pattern scanning) and naming the known contract
# gap up front the way T07's spec named the missing ctx parameter.
description = """
Implement the `vg` CLI (`crates/vg-cli`) and the Claude Code hook adapter
(`crates/vg-adapters-claude`), wiring the now-complete `vg_core::{scan, mask, rehydrate}`
pipeline (contract v1.1). The adapter contract is `interface-contracts.md` section 8; follow it
exactly (hooks: `UserPromptSubmit` -> mask(prompt), `PreToolUse`/`PostToolUse` -> mask(tool_io);
exit codes 0 pass-through / 2 transformed / 1 block-with-reason-to-stderr; the adapter NEVER
calls `vault.resolve` -- demask is only the separate user-invoked `vg demask` flow).

**A known contract gap you will hit immediately -- resolve it via the contract-change protocol
(v1.1 -> v1.2, `agent-factory-plan.md` section 6, documented in interface-contracts.md, never
silently):** the frozen `rehydrate(masked: &str, dest, actor)` cannot reach the vault, and the
T07 review banked a HARD requirement: demask must resolve **exclusively via the pack's
`MappingRef`s -- never by pattern-scanning text for placeholder-shaped strings** (input already
containing `EMAIL_001`-shaped text is indistinguishable from pipeline output). Since
`MaskedPack.mapping_refs` carries refs but not the display<->ref association needed to locate
placeholders, the recommended v1.2 shape is: (a) `MaskedPack` gains an additive field binding
each display string to its `MappingRef` (populated by `mask` -- it has both at intern time),
and (b) `rehydrate` is re-signed to take the pack + `&Policy` + `&Namespace` + dest + actor,
keeping the existing hard-deny check FIRST (before any vault/policy consultation), then
`policy.engine.demask_allowed`, then per-ref `policy.vault.resolve`, substituting only the
displays the pack itself minted. Document the inherent residual: raw input that collides with
a pack-minted display substitutes too -- that is in-band text masking's nature, recorded not
hidden. Use your judgment on exact shapes; record every call in docs/decisions.md.

CLI subcommands (clap is already in the workspace lock via criterion; add it as a real
dependency): `vg run -- <cmd...>` (wrapper: writes a hook-config for the wrapped Claude Code
invocation, prints the pre-send summary from MaskStats, passes through env for the Bedrock
path -- configuring the wrapped CLI, e.g. CLAUDE_CODE_USE_BEDROCK=1 pass-through, is the
"Bedrock masked-request path"; do NOT build an HTTP client); `vg inspect <file>` (what WOULD
be masked -- runs scan+policy classification, prints findings/classes, changes nothing);
`vg diff --masked <file>` (show original vs masked); `vg demask --from <file> --to <dest>`
(the explicit gate above); `vg audit last|<id>` (read the audit sink); `vg policy check`
(load + validate the layered packs, print the resolved summary); `vg vault stats`
(mapping_count, no values). Every command help text complete.

State/wiring: the hooks and CLI need a consistent place for the vault DB, audit log, policy
layers, and persisted packs (demask needs the pack from a prior mask). A repo-local
`.veilgremlin/` state dir (gitignored) with the global policy at a well-known path is a
reasonable choice; hooks read JSON on stdin per Claude Code's hook protocol and must emit
ONLY the transformed text on stdout when exiting 2. Keep per-hook-invocation overhead
minimal and measured -- hooks sit on the interactive hot path, and a cold SQLCipher open +
keychain fetch per invocation is exactly the kind of perceptible friction the acceptance
criterion below exists to catch; record cold-vs-warm numbers where feasible.

Tests: integration tests driving the compiled `vg` binary (assert_cmd or std::process) over a
temp state dir with the real crates -- a masked round trip (mask via hook-shaped input, then
`vg demask --to local_patch` restores the original); the demask gate DENIES
remote_model_prompt/observability_sink regardless of actor; a spoofed placeholder-shaped
string NOT minted by the pack is left untouched by demask; `vg inspect` output contains
classes and counts but never a raw detected value where the class is redact/mask.

This is a one-shot headless dispatch: no follow-up channel. Use your judgment on exact
naming/shapes, follow the frozen contracts and existing conventions, record every assumption
in docs/decisions.md rather than asking. NOTE: the human-in-the-loop UX session in the
acceptance criteria happens at review time with the human -- your job is to make it possible
(working hooks + a docs/runbook-hooks.md walkthrough for wiring them into a real Claude Code
session), not to claim it happened.
"""
acceptance = "masked round trip to Bedrock path; demask gate denies remote_model_prompt; CLI help complete. ADDED 2026-07-16: a human runs a real interactive Claude Code session with the hooks wired in against realistic prompts/tool output containing masking-worthy content, and explicitly confirms no perceptible added latency or workflow friction -- record the session and the human's honest verdict in docs/decisions.md, not just a benchmark number. If it IS perceptible, that's a real finding to fix or escalate, not a criterion to quietly relax."
context = ["docs/architecture/interface-contracts.md"]

# --- Epic E5 — Evaluation & gate (Wave D) -----------------------------------

[[task]]
id = "T10"
wave = "D"
depends_on = ["T09"]
task_type = "code-implement"
engine = "claude-cli"
description = "Seeded corpus (labelled) + metrics harness (recall/FP/consistency/latency/zero-raw-PII); incident-log demo fixture; Go/No-Go report generator."
acceptance = "report emits all Go/No-Go metrics; demo scenario runs; thresholds evaluated."
context = ["docs/spec/requirements-and-design-spec.md"]

[[task]]
id = "T11"
wave = "D"
depends_on = ["T10"]
task_type = "code-review"
engine = "codex-cli"
description = "Review + /security-review on the full diff (T01-T10); privacy + supply-chain criteria; sign-off. Deliberately a different engine than the one that wrote the code."
acceptance = "review findings triaged; gates green; human approval recorded."
context = ["docs/architecture/agent-factory-plan.md", "docs/spec/requirements-and-design-spec.md"]
