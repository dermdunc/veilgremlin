//! `vg` — VeilGremlin CLI entry point.
//!
//! Real subcommands (`vg run -- claude ...`, `vg demask`) land in Task T09, wired
//! against `vg-core` and `vg-adapters-claude`. Scaffolded here (Task T01) so the
//! workspace has a binary crate to build/lint/release from day one.

fn main() {
    println!("vg: not yet implemented (workspace scaffolded in Task T01; CLI lands in Task T09)");
}
