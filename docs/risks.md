# Risks: VeilGremlin

## Risk Register

Machine-readable risk state lives in `.hekton/risk-register.yaml`. Keep this
Markdown file as the human-readable explanation of material risks and mitigations.

| ID | Date | Risk | Impact | Likelihood | Mitigation | Status |
|---|---|---|---|---|---|---|
| RISK-0001 | 2026-06-30 | Initial governance baseline needs first human/agent review | Medium | Medium | Run governance preflight and end-session review during the first material session | Open |
| RISK-0002 | 2026-06-30 | Hot-path latency regresses past budget (kills adoption) | High | Medium | Criterion benches gated in CI; p95<25ms assembly / <50ms e2e; arena alloc; compiled regex sets. **Resolved 2026-07-18 (T10):** detectors (T03) and a plain-`#[test]` CI latency-regression gate exist and pass on every PR; the T10 eval measured real latency (cold-hook e2e p95 22.44 ms, in-process detect p95 ~12 ms), comfortably under the 50 ms budget. Latency is not the current risk; precision is (RISK-0004). | Resolved 2026-07-18 |
| RISK-0003 | 2026-06-30 | Detector recall insufficient (raw PII leaks) | High | Medium | Layered deterministic detectors + warm-path GLiNER; red-team corpus; secret recall ≥99% / PII ≥95% gates; conservative artefact blocking | Open |
| RISK-0004 | 2026-06-30 | False positives degrade utility; developers disable the tool | High | High | FP budget <3%; allow-lists; `vg inspect`/`diff` transparency. **Measured 2026-07-18 (T10) — this is the live NO-GO driver:** the eval harness returned false-positive-rate **16.7%** against the `<3%` gate (entropy 13.3%, phone 40%), so the Go/No-Go verdict is NO-GO. See `docs/decisions.md` (2026-07-18 T10 entry) and `vg bench`. Remediation (entropy/phone precision, collision-avoiding minting) is required before T11 sign-off. **Sharpened 2026-07-21:** the un-dilutable benign-slice numerator (T10 doubt-pass round 2, 2026-07-19) is entropy: 1 (commit SHA), phone: 2 (ISBN/zip) — format-collision, not entity ambiguity; an entity-relationship-graph approach was considered and rejected for this gate (see `docs/decisions.md`). Fix is targeted SHA-shape / ISBN-checksum / zip-shape exclusion in the two detectors — see `docs/next-actions.md`. **Fixed, doubt-passed twice, pending merge (2026-07-21/22, branch `agent/claude/t10-fp-detector-fixes`):** `vg bench` verdict is now **GO**, false-positive-rate **0.0%** (was 16.7%). Two doubt-driven-development rounds (round 1: single-model + Codex cross-model, round 2: single-model targeted at round 1's own fix code) found 14 total findings, all closed with regression tests — see `docs/decisions.md` (2026-07-22 entries). Not yet merged to `main` — pending human review per this repo's standing precision-change discipline; risk stays Open until landed. | Open — fix doubt-passed twice, pending human review |
| RISK-0005 | 2026-06-30 | Demask re-exposes values to a cloud model | Critical | Low | Destination-typed deny; `remote_model_prompt` hard-deny; model has no vault handle; rehydration is a separate local gate | Open |
| RISK-0006 | 2026-06-30 | Vault key mishandling / plaintext at rest | Critical | Low | OS-keychain key wrap; `zeroize`; never persist key plaintext; SQLCipher AES-256 | Open |
| RISK-0007 | 2026-06-30 | Supply-chain compromise of the privacy binary | Critical | Low | Signed releases, SBOM, reproducible builds, no telemetry, `cargo-deny`/`cargo-audit`, signed policy packs | Open |
| RISK-0008 | 2026-06-30 | Multi-agent build: squads diverge on shared types | Low | Low | Contract-first freeze (Wave A); contract-change protocol; ownership rule (edit only your crate). **Downgraded 2026-07-18:** the build completed T01–T10 with the contract held through v1.4 under the change protocol; no uncoordinated divergence occurred. | Resolved 2026-07-18 |
| RISK-0009 | 2026-06-30 | Over-claiming GDPR/AI-Act compliance in public copy | High | Low | Binding positioning note in spec/README; "supporting control" language only; review gate | Open |
| RISK-0010 | 2026-06-30 | Git remote authentication mismatch blocked pushes early in setup | Low | High | Standardise the `dermdunc/veilgremlin` remote and auth | Resolved 2026-07-04 — moot once the repo settled on `dermdunc/veilgremlin` (public) |
| RISK-0011 | 2026-07-17 | T08 (`vg-parsers`) built without a compiler in-session; adds 4 new deps so `Cargo.lock` is stale and all `--locked` CI jobs fail until regenerated; tree-sitter version pair unverified | Medium | High | At PR review: run `cargo build` to refresh + commit `Cargo.lock`, then `cargo test -p vg-parsers && cargo clippy --all-targets -- -D warnings && cargo fmt --check`; confirm `tree-sitter`/`tree-sitter-rust` pair resolves (adjust pins, not the `rust.rs` call site, if not). Same headless-no-compiler pattern as T04, plus a Cargo.lock action T04 didn't have. **Resolved 2026-07-18:** T08 merged; `Cargo.lock` regenerated at review (commit `aed7ded`); the tree-sitter pair resolves and CI `--locked` jobs pass. | Resolved 2026-07-18 |
